# E14 Auditoría — Dependencies

# FastAPI and server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# HTTP client with browser fingerprinting (Akamai bypass)
curl_cffi>=0.7.0

# Task queue
celery>=5.4.0
redis>=5.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# PDF processing
pymupdf>=1.24.0

# Image processing
Pillow>=10.3.0
numpy>=1.26.0
opencv-python>=4.9.0.80
pyzbar>=0.1.9  # QR/barcode detection (R-17/B-6) — requires system libzbar0

# OCR (primary engine)
paddleocr>=2.8.0
paddlepaddle>=2.6.0  # CPU version

# Object storage (S3-compatible)
boto3>=1.34.0
minio>=7.2.0

# Rate limiting
slowapi>=0.1.9

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Monitoring and logging
prometheus-client>=0.20.0
structlog>=24.1.0

# Utilities
tenacity>=8.2.0  # Retry logic
httpx>=0.27.0    # Async HTTP client

# Development and testing
